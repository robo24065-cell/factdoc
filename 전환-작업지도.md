# FactDoc → 사실은ON 전환 작업지도

> 원본: `C:\Users\PC\Downloads\공모전` (FactDoc, 병무청·질병관리청 경진대회 — 중단)
> 목표: **같은 레포·같은 배포판을 통일부 공모전용 '사실은ON'으로 전환** (새 페이지 안 만듦)
> 생성일: 2026-08-09

---

## 0. 왜 전환이 쉬운가 — 구조가 1:1로 겹침

| FactDoc (기존) | 사실은ON (전환 후) | 재사용 |
|---|---|---|
| 4단계 판정: 사실 / 부분적·과장 / 근거없음·허위 / 공식근거없음·보류 | 4등급: 확인됨 / 공식자료와 다름 / 부분일치 / 확인불가 | **엔진 그대로** |
| LLM은 판정 안 함 (룰+클레임그래프가 판정) | 동일 | **설계 그대로** |
| Why-Trace (판정 경로 시각화) | 동일 (근거 투명성 = 리스크 방어) | **그대로** |
| 검증 티어 (자동·미검증 / 검증완료) | 동일 (휴먼인더루프 승인 큐) | **그대로** |
| 명예훼손 회피: 주장(claim)만 검증 | 동일 — 정치 리스크 방어의 핵심 | **그대로** |
| 근거수준을 출처가 결정 | 통일부 자료 종류별 근거수준 | **룰 테이블만 교체** |
| 질병관리청 코퍼스 + 식약처 룰 | 통일부 보도설명자료 + 북한동향/합의서/통계 | **데이터만 교체** |
| 한국어 건강 온톨로지(동의어) | 남북 용어 온톨로지(용어사전·언어비교 API) | **소스만 교체** |

→ **바꿀 것은 도메인 데이터·어휘·룰 테이블. 아키텍처·판정엔진·UI 골격은 그대로.**

---

## 1. 배포 인프라 (기존 것 그대로 사용)

| 항목 | 값 |
|---|---|
| GitHub 레포 | `robo24065-cell/factdoc` (origin, main) |
| 호스팅 | Cloudflare Pages (GitHub 푸시 시 자동 배포) |
| DB/벡터 | Supabase project ref `fcezfdnymianzxrezkdf` |
| Edge Functions | `parse-claim` `explain-info` `explain-verdict` `embed-text` `review-answer` (Deno) |
| LLM | Gemini Flash (Edge Function 안에서만 호출) |
| 임베딩 | BGE-m3 1024차원 |

**키 원본**: `api.txt`, `배포-가이드.md` — 둘 다 `.gitignore`로 커밋 차단됨(확인 완료). 절대 이 두 파일을 커밋하지 말 것.

---

## 2. 추출된 것 (187개 파일 / 3.9MB)

```
추출폴더/
├── .git/                    ← 원본 이력 그대로 (origin=factdoc, main, clean)
├── .github/workflows/       ← 데이터 자동갱신 5종 (통일부 API용으로 재작성 대상)
├── frontend/
│   ├── src/engine/          ← 판정엔진 (코어 재사용 + 도메인 KB 교체)
│   ├── src/lib/             ← Supabase·캐시·검색·임베딩 (거의 그대로)
│   ├── src/components/      ← Panel·InfoTip·WhyTrace·Highlight 등 (그대로)
│   ├── src/layouts/         ← Consumer/Admin (그대로)
│   ├── src/pages/           ← Home·Review·Eval 재사용 / 보건 전용 페이지 교체
│   └── (vite·tsconfig·package.json·eslint·index.html)
├── supabase/
│   ├── functions/           ← Gemini 오케스트레이션 5종 (프롬프트만 교체)
│   └── migrations/ 0001~0010 ← 스키마 도메인 무관, 그대로 사용
├── scripts/                 ← 수집·임베딩·평가 하니스 (fetch-* 만 교체)
├── api.txt / 배포-가이드.md   ← 🔒 키 원본 (gitignore됨)
└── CLAUDE.md / PLAN.md / PROGRESS.md / README.md  ← ⚠ 전부 병무청 대회 기준. 재작성 필요
```

**제외한 것**: `node_modules/` `dist/` `corpus_raw/` `.temp/`, 병무청 공모전 PDF 3종
(PDF 3개는 git에 tracked 상태여서 `git status`에 삭제로 표시됨 — 의도된 삭제)

---

## 3. 도메인 교체 지도

### 🟢 그대로 (수정 불필요)
- `supabase/migrations/*` — source_doc, chunk, claim_triple, ontology_term, query_log, verdict_cache, answer_feedback 전부 도메인 무관
- `lib/`: supabase.ts, db.ts(코퍼스 참조만 교체), cache.ts, embed.ts, search.ts, fusion.ts, feedback.ts, parseRemote.ts
- `components/`: Panel, InfoTip, WhyTrace, Highlight, FontScale, icons
- `layouts/`: ConsumerLayout, AdminLayout
- `engine/`: types.ts, intent.ts, parse.ts, stats.ts, baselines.ts, fromRaw.ts, explainLocal.ts
- `scripts/`: ingest.ts, embed.mjs, embed-cache.mjs, seed.ts, revalidate.ts, ablation.mjs, gen-eval-dataset.mjs, parse-eval.mjs, finalize-eval.mjs, test-*.mjs
- vite/tsconfig/eslint/package.json — 스택 동일

### 🟡 교체 (구조 유지, 내용만)
| 파일 | 현재 | 전환 후 |
|---|---|---|
| `engine/ontology.ts` `relationLex.ts` | 건강 동의어·관계어휘 | 남북 용어(용어사전·언어비교 API) — `오물풍선↔대남 쓰레기 풍선` |
| `engine/judge.ts` `claimGraph*.ts` | 완치룰·식약처룰 | 통일부 보도설명자료 매칭룰·수치 대조룰 |
| `engine/guidance.ts` | 의료 면책·상담안내 | 정치중립 면책·자료 기준일 고지 |
| `supabase/functions/*/index.ts` | 건강 파싱/설명 프롬프트 | 북한·통일 주장 파싱/설명 프롬프트 |
| `.github/workflows/*.yml` | KDCA·KOSIS·네이버·조달청 수집 | 통일부 보도자료·북한동향·용어사전 API 수집 |
| `scripts/fetch-*.mjs` | 보건 데이터 수집기 | 통일부 API 수집기 (fetch-corpus.ts 패턴 재사용) |
| `pages/Home.tsx` | 건강 팩트체크 UI | 사실은ON 팩트카드 UI (골격 유지) |

### 🔴 삭제 (보건 전용 — 다만 import 그래프 확인 후)
`engine/`: drug-kb.ts(101KB), drug.ts, food-kb*.ts(~180KB), food.ts, ingredients.ts, mfds-functions.ts, mfdsRules.ts, symptoms.ts, disease-rumors.ts, kdca-corpus.ts(1.75MB), ontology-eid.ts, ontology-foods.ts
`data/`: bodyspec, checkup-dist, death-*, eid-region(281KB), naver-trends, procurement
`pages/`: Disease, InfectiousMap, Forecast, Trending, Me, Dashboard, Strategy
`components/`: CheckupPercentile, DeathRegionPanel, DeathTrendPanel, DemandModelPanel, EidDeathPanel, PersonalRisk
`lib/`: checkup, eidStats, demandModel, uniformDemand, supplyForecast, infectionReport, prevention

> ⚠ **주의**: `judge.ts`가 `food`/`drug`/`mfds`/`symptoms`를 직접 import 중.
> 삭제 전 해당 import를 통일부 룰 모듈로 치환해야 빌드가 안 깨짐. 한 번에 지우지 말고
> ① 통일부 룰 모듈 추가 → ② judge.ts import 교체 → ③ 구 모듈 삭제 순서로.

### 🆕 신규 추가
- `engine/nk-lexicon.ts` — 남북 용어 정규화 (용어사전·언어비교 API 산출물)
- `engine/mou-rules.ts` — 통일부 보도설명자료 매칭·수치 대조 룰
- `scripts/fetch-mou-briefings.mjs` — 보도자료 API 수집 (보도설명자료 필터)
- `scripts/fetch-nk-trends.mjs` — 북한동향 일일/주별/월별
- `scripts/fetch-nk-lexicon.mjs` — 용어사전 + 남북한 언어비교

---

## 4. 깃 푸시 로직

`.git`을 그대로 옮겨왔으므로 **추가 설정 없이 바로 푸시 가능**. 원본 `공모전` 폴더는 손대지 않음.

```powershell
cd "C:\Users\PC\Downloads\2026년 통일부 공공데이터 활용 공모전\추출폴더"

# 1) 상태 확인 — api.txt / 배포-가이드.md / .env 가 목록에 없어야 정상
git status --short

# 2) 시크릿 차단 재확인 (출력이 있으면 차단된 것 = 정상)
git check-ignore -v api.txt "배포-가이드.md" frontend/.env

# 3) 커밋 & 푸시
git add -A
git commit -m "chore: 통일부 공모전용으로 전환 시작 (사실은ON)"
git push origin main
```

→ 푸시하면 **Cloudflare Pages가 자동 재배포**. 별도 연동 작업 없음.

**푸시 전 필수 점검**
- [ ] `git status`에 `api.txt` / `배포-가이드.md` / `frontend/.env` 없음
- [ ] 소스에 키 하드코딩 없음 (`git diff --cached | Select-String "AIza|sbp_|github_pat"`)
- [ ] 빌드 통과 (`cd frontend; npm install; npm run build`)

---

## 5. 진행 상태 (2026-08-10 기준)

> 실제 구현은 §3의 "기존 judge.ts를 개조" 경로가 아니라 **`nk-` 접두 엔진을 새로 얹는 경로**로 갔다.
> 레거시 `engine/*.ts`(judge/parse/food-kb…)는 손대지 않고 `/legacy`로 밀어둔 상태 — 삭제하지 않았다.

### ✅ 완료
- **CLAUDE.md 재작성** — 사실은ON 기준으로 전면 교체
- **엔진** `engine/nk-search.mjs` `nk-time.mjs` `nk-normalize.mjs` `nk-llm.mjs`
  - as-of 3상태(live/stale/frozen) · 주제 frozen 공지 · 단위 가족 · 차원 집계 · 연혁 · 도메인 게이트
- **데이터** `scripts/nk-catalog.mjs`(24종 ready + 5종 pending) → `ingest-nk.mjs`
  - 레코드 12,690 · 수치 37,912 · 엔티티 721
- **LLM 중간계층** Gemini 폴백 3모델 × 키 5개 로테이션, 스키마 밖이면 폐기→규칙
- **웹 화면** `pages/SasilOn.tsx` (`/`) + 전용 `layouts/SasilOnLayout.tsx`, 레거시 Home은 `/legacy`
  - `build-web-index.mjs` → `public/nk-index.json` 13.5MB (gzip 0.9MB)
  - `?q=` 퍼머링크 — 팩트체크 결과 공유용 (캡처 자동화에도 씀)

  > ⚠ **1차 구현 전량 폐기하고 재작성함.** 손으로 쓴 CSS를 `App.css`에 넣었는데
  > **그 파일을 import 하는 코드가 프로젝트에 하나도 없었다** → 스타일이 로드된 적이 없음.
  > 게다가 이 프로젝트는 Tailwind v4를 쓴다. `get_page_text`로 텍스트만 확인하고
  > "완료"라고 판단한 것이 원인. **화면 작업은 반드시 픽셀을 봐야 한다.**

- **캡처 도구** `scripts/shot.mjs` — CDP 디바이스 에뮬레이션으로 10종 자동 캡처 + 가로 오버플로 감사
  - `chrome --headless --window-size=375,…` 는 이 플랫폼에서 **창 너비를 500px로 강제**한다(실측).
    375로 요청하면 500px 레이아웃을 375px로 잘라낸 이미지가 나와 멀쩡한 화면이 깨져 보인다. → CDP 필수.
- **조사 자동 선택** `josa()` — "가장 많은 연령대**은**" 같은 문장을 막는다. 지표명·차원명이 데이터에서 오므로 필수

- **검색·판정 결함 6종 수정** (사용자가 `북한 핵`/`우라늄 공장 폐수`로 실패를 지적)
  | 결함 | 원인 | 수정 |
  |---|---|---|
  | `북한 핵`과 `북한방사능`이 같은 결과 | `tokenize`의 `[가-힣]{2,}` 가 1음절을 버림. 코퍼스에 핵 640건 | 1음절 화이트리스트 `MONO` + 질의측 어휘 확장(`핵`→`북핵·비핵화·핵실험`) |
  | 인물 721명이 모든 `북한*` 질의에 혼입 | `sourceName`("북한 주요 인물")을 **본문처럼 색인** | `own`(제목+본문) 분리 — sourceName 은 근거 자격에서 제외 |
  | 무관한 걸 "근거 6건"으로 포장 | 관련성 바닥 없음 | 변별 토큰 커버리지 → `own` → `REL_FLOOR 0.25` 3단 필터 |
  | 웹에서만 `우라늄 공장 폐수` 실패 | `build-web-index.mjs` 가 body 220자 절단 → **브라우저 검색 인덱스가 잘린 텍스트로 생성**. 어휘 48.6% 소실, 북한개황 128건 전량 검색 불능 | 표시용 `body` / 검색용 `st` 분리 (163건 부착, gzip 0.9→1.04MB) |
  | `방사능 폐수`에 "다루는 분야가 아닙니다" | 주제에 대한 **거짓 단정**. 통일부는 평산 우라늄공장 폐수로 보도설명자료를 냈다 | 질의에 대한 진술로 교체 — "북한·통일 자료와 연결되는 내용을 찾지 못했습니다" |
  | 화면이 스스로와 모순 | 헤드라인은 "근거 아님"인데 아래는 "이 답변의 근거 1종" | `refOnly` 로 배지·섹션·조항라벨·요약스트립 전부 '참고'로 정합화 |

  > ⚠ **핵심 위험이었던 것**: 관련성 문턱을 올리면 이 프로젝트가 갈아엎은 "확인이 어렵다" 과잉회피가 되살아난다.
  > 그래서 `wild-set.mjs` 에 **과잉주장 감시 10건 + 과잉회피 감시 10건을 같은 세트에** 넣었다.
  > 한쪽만 감시하면 문턱이 계속 한 방향으로 밀린다.
  > 신규 20건은 **패치 전 42/52(81%) → 패치 후 52/52** — 실제로 변별하는 세트임을 확인함.
- **검증** `eval.mjs` 48/48 · `wild.mjs` 32/32 · `verify-nk-db.mjs` 18/18
- **DB 스키마** `0011_nk_tree.sql` `0012_verify_layer.sql`
  - PGlite(PostgreSQL 16)에 마이그레이션+적재 전 과정을 재현해 확정. 실 DB 없이도 스키마가 검증됨
  - 확정 중 교정: `measure.dims` 추가 / `dataset.status`·`search_priority` 추가 / `as_of` NOT NULL을 `status='ready'` 조건부로 완화(pending은 기준일이 없음)
- **레거시 워크플로 5종 중지** — `schedule:` 주석 처리, `workflow_dispatch`만 남김 (파일은 보존)

### ✅ Supabase 적재 완료 (2026-08-10)
프로젝트 `fcezfdnymianzxrezkdf` 는 **삭제된 게 아니라 일시정지** 상태였다. 사용자가 리줌 → 즉시 연결됨.
```
session pooler aws-1-ap-northeast-2:5432 → PostgreSQL 17.6
node scripts/load-nk-db.mjs → 6.1초에 적재 완료
레코드 12,690 · 수치 37,912 · 엔티티 721 · record_entity 4,968
실 DB 불변식 검증 11/11 (frozen 사유 강제 / kind='news' 거부 / asof_notice / ltree 롤업 / dims 분포 / 전문검색)
```

> ⚠️ **진단 실패 기록 — 같은 실수를 반복하지 말 것.**
> 나는 이걸 "삭제됨"으로 단정했다. 근거는 `EAI_AGAIN` + pooler `tenant not found` + Management API 401 이었다.
> **`EAI_AGAIN` 은 "도메인 없음"이 아니라 "DNS 조회 일시 실패"다.** NXDOMAIN(`ENOTFOUND`)과 전혀 다르다.
> 일시정지된 Supabase 는 DNS·pooler 가 일시적으로 응답하지 않을 수 있고, 액세스 토큰 만료는 프로젝트 존재와 무관하다.
> 세 신호 모두 "정지"와 "삭제"를 구분하지 못하는데 삭제로 결론냈다.
> → **없음을 단정하기 전에 대조군을 두라.** 존재하지 않는 ref(`zzzz….supabase.co`)와 비교했더니
>   그쪽만 `ENOTFOUND` 였고 실제 프로젝트는 IP 가 나왔다. 이 서비스가 stale 과 frozen 을 구분하는 것과 같은 원칙이다.

### ⬜ 남은 것
1. 새 Supabase 발급 → `load-nk-db.mjs` 실행
2. 뉴스 레이더(`nk-issue-radar.mjs`)를 `news_item`/`issue`에 연결 (TOP 10 정밀도 현재 ~50% — 노이즈 필터 개선)
3. `entity_alias`가 5건뿐 — 남북 용어 대응(`오물풍선 ↔ 대남 쓰레기 풍선`)을 채워야 스키마 주석값을 함
4. 통일부 API 복구 시 pending 5종 → ready
5. README.md 재작성
6. 푸시 → Cloudflare 배포 확인 (로컬이 origin보다 50커밋 뒤처짐 — 푸시는 사용자 확인 후)
7. **서식3 기획서 작성** (A4 10쪽 이내, 마감 2026-08-28 18:00)

---

## 6. 주의사항

- **원본 폴더(`Downloads\공모전`)는 그대로 보존** — 롤백 기준점. 작업은 추출폴더에서만.
- **레포를 덮어쓰면 FactDoc 이력 위에 쌓임** — 원본 FactDoc 배포는 사라짐. 사용자 확인: "저 페이지도 아무도 안 씀" → 진행 OK.
- **동일 아이템 중복 출품 규정**: FactDoc이 병무청 대회에 **입상**한 이력이 있으면 문제. 중단(미출품)이면 무관.
- 통일부 데이터 라이선스(공공누리 유형) 확인 필요 — 원문 통째 재배포 금지 시 FactDoc과 동일하게 "출처 포인터+짧은 인용+자체 요약" 모델 적용.
