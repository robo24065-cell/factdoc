// 빌드 산출물: scripts/parse-eval.mjs (오프라인 1회). 평가셋 Gemini 파싱 결과(raw). ⚠ 수기 편집 금지.
// (실행 전 플레이스홀더 — parse-eval.mjs 실행 시 실제 파싱으로 덮어씀. 비면 규칙 파서만 사용)
import type { RawClaim } from '../fromRaw'

export const EVAL_RAW: Record<string, RawClaim[]> = {}
