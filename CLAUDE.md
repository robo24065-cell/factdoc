# 사실은ON — 국민이 묻고 국가 공식자료가 답하는 북한·통일 팩트체커

> **한 줄 정의**: 북한·통일 관련 주장과 개떡같은 자연어 질문을, **통일부 공공데이터**에 대조해 답하되 **"그 자료가 언제 것인지(as-of)"를 반드시 함께 말하는** 서비스.
> **출품 대회**: **2026년 통일부 공공데이터 활용 공모전 — ① 아이디어 기획 부문**
> **마감**: 2026-08-28(목) 18:00 · 제출: `data@unikorea.go.kr` · 서식3 기획서(A4 10쪽 이내, 휴먼명조 14pt, 줄간격 160%)

---

## ⚠️ 이 폴더는 '전환된' 프로젝트다 (가장 먼저 읽을 것)

이 코드베이스는 원래 **FactDoc**(2026 병무청·방위사업청·질병관리청 합동 대회, 건강정보 팩트체커)이었고 **중단**됐다.
사용자가 그 프로젝트의 **깃 저장소 / Cloudflare Pages 배포 / Supabase 인스턴스 / Gemini 키**를 그대로 재활용해
**사실은ON으로 둔갑**시키기로 결정했다. 새 페이지를 파지 않는다.

**그래서 이 폴더에는 두 세대의 파일이 섞여 있다.**

| | 현행 (사실은ON) | 레거시 (FactDoc — 건드리지 말 것) |
|---|---|---|
| 엔진 | `frontend/src/engine/nk-*.mjs` | `frontend/src/engine/*.ts` (judge/parse/intent/food-kb/…) |
| 스크립트 | `scripts/nk-*.mjs`, `eval.mjs`, `wild.mjs`, `build-web-index.mjs` | `scripts/fetch-kdca-*`, `fetch-mfds-*`, `fetch-mma-*`, `embed*`, `ingest.ts` |
| 마이그레이션 | `0011_nk_tree.sql`, `0012_verify_layer.sql` | `0001`~`0010` |
| 화면 | **`GohyangOn.tsx` (`/` ★메인)** · `SasilOn.tsx` (`/factcheck`) | `Home` (`/legacy`) |
| 문서 | 이 문서, `전환-작업지도.md` | `PLAN.md`, `PROGRESS.md`, `데이터-API-조사.md`(질병청 부분) |

**레거시를 삭제하지 마라.** 배포/CI가 아직 일부 참조하고, 사용자가 지운 적 없다.
새 작업은 전부 `nk-` 접두 / `0011+` 마이그레이션 쪽에 붙인다.

---

## ⚡ 세션 재개 프로토콜

1. 이 문서를 끝까지 읽는다 (단일 진실 소스).
2. **`전환-작업지도.md`**를 읽는다 — 남은 전환 작업 체크리스트.
3. 남은 작업부터 이어간다. **기존 구조를 임의로 갈아엎지 않는다.**
4. 데이터·as-of 판단이 걸리면 **`scripts/nk-catalog.mjs`가 유일한 근거**다 (§4).
5. 엔진을 고쳤으면 **반드시** `node scripts/eval.mjs`(48건) + `node scripts/wild.mjs`(32건)를 돌려 100%를 유지한다.

### 🔐 절대 규칙 — 키
사용자 지시: *"임시키파일은뭐야? 놔둬그거. **깃헙에만 안업로드하면되** 키값은."*
- 로컬 키 파일은 **지우지 말 것**: `api.txt`, `배포-가이드.md`, `frontend/.env`, `.gemini-keys.tmp.json`, `*keys*.json`, `*.tmp.json`
- 전부 `.gitignore` 처리됨(`git check-ignore`로 검증 완료). **커밋·푸시에 절대 포함시키지 말 것.**
- 새 키 파일을 만들면 즉시 `.gitignore`에 추가하고 `git check-ignore`로 확인한다.

---

## 1. 대회 정보

- **부문**: ① **아이디어 기획** (② 서비스 개발 아님 — *작동 시제품은 필수가 아니지만, 실현가능성 입증용으로 만들고 있다*)
- **심사 (100점)**

  > 아래는 공모전 안내문 PDF **원문 그대로**다. 추측으로 바꾸지 말 것.

  | 평가지표 | 배점 | 안내문 원문 | 우리 대응 |
  |---|---|---|---|
  | 적합성 | 30 | 통일부 공공데이터를 아이디어에 얼마나 적절하게 활용했는지 | 통일부 데이터 24종 실적재 + REST API 18종 중 다수 수집 |
  | 창의성 | 30 | 아이디어의 독창성 및 차별성 | as-of 3상태 모델(§3) — 기존 팩트체커에 없는 축 |
  | 구체성 | 20 | 아이디어가 현실적으로 구현 가능하고 구체적인지 | 실제 동작하는 배포본 + 회귀평가 수치 |
  | 효과성 | 20 | 아이디어 도입 시 예상되는 기대효과 | 「#사실은 이렇습니다」의 데이터·AI 확장 |

- **팀 규모**: 최대 **4인** (개인도 가능)
- **시상**: 최우수 100만원 · 우수 50만원 · 장려 30만원 3팀 — 전부 통일부장관상
- **일정**: 접수 ~8.28(금) 18:00 → 서류심사 8.31~9.3 → 발표 9.10(목)
- **필수**: 공공데이터포털에 개방된 **통일부 공공데이터 1건 이상** 활용 (타 기관·민간 데이터 연계 가능)

- **핵심 판단**: 20점짜리 '구체성'은 말로 못 딴다 → **돌아가는 물건**을 만들어 기획서에 캡처·수치로 박는다.
- **회피한 주제**: '정착나침반'류 탈북민 정착 추천 = 2025년 장려상 「나와 닮은 선배가 걸어온 길」과 사실상 동일 → 제외.

---

## 1a. ★ 화면 구성 — 2026-08-19 변경 (사용자 지시)

> *"사실은on 팩트체커를 누르면 들어가게 되는 페이지로 사이드페이지로 빼버리고 우리가 지금하는게 메인페이지가 되도록 구성. 지금 뭐 클릭해서라도 구축하고 있는 페이지로 접근할수있는 루트가 없는데"*

**★ 사이트 구조는 `사이트구조.md` 가 단일 진실 소스다** (2026-08-19 사용자 지시: "실 이용자가 접근하기 편하게").
요약: 한 페이지 = 한 질문. `/`(홈: 숫자+고향버튼+입구3) · `/map` · `/clock` · `/action` · `/archive` · `/factcheck` · `/sources`.
모바일은 하단 탭 4개. `/gohyang` → `/` 리다이렉트.

- 이동은 `SasilOnLayout.tsx` 헤더의 `<Nav>` 두 칸으로 한다. **이 내비게이션을 없애면 지도 화면에 들어갈 길이 사라진다.**
- 지도 → 팩트체커: 헤더 링크 + 지역 패널의 「사실은ON에서 검색하기」(`/factcheck?q=지역명`).
- 레거시 화면들의 `/?q=` 는 전부 `/factcheck?q=` 로 옮겼다(의도 보존).

### 고향ON 3축 (사용자 승인 방향)
1. **소멸 시계** — 등록현황 월별 98개월 × 통계청 완전생명표(KOSIS) → 잔존 추계. 1만 명 하회 **2038~2041년**(두 시나리오 범위).
2. **살아있는 고향 페이지** — 지역 클릭 → 이산가족 고향별 생존자·탈북민 출신지·연표/보도/동향 건수·**실시간 기상**·frozen 공지.
3. **후손 다리** — 제4차 실태조사(2024) 후손 문항. 간극 3종 + 「기록물 수집 보존」 59.9% 요구.

**기상만은 빌드에 굽지 않는다.** NOAA GSOD·ISD는 2026년 자료가 없고(HTTP 404, 최신 2025-08-24), 기상청 계열은 전부 로그인 필수다. 그래서 **Open-Meteo**를 브라우저가 직접 부른다(키 없음, 13지역 1회 호출). 저장하면 그 순간 stale 이 되는 유일한 계열이라 실시간이 as-of 를 지키는 방법이다.

---

## 2. 제품 개요

- **문제**: 북한·통일 정보는 (a) 데이터가 **어느 시점에서 끊겼는지**를 사용자가 모르고, (b) 질문이 정형화돼 있지 않다.
  기존 시스템은 대부분의 비정형 질문에 **"확인이 어렵습니다"**로 도망친다(= FactDoc이 실패한 지점).
- **해결**: 통일부 공식자료를 트리 인덱싱해 두고, **개떡같이 물어도 요지를 파악**해 답하되 **자료 기준일을 항상 동반**한다.
- **모태**: 통일부가 이미 운영 중인 「#사실은 이렇습니다」(보도설명자료). 그 업무를 데이터·AI로 확장하는 포지션 — 새 업무를 만들자는 게 아님.

---

## 3. ★ 핵심 발명 — as-of 3상태 모델 (창의성 30점의 축)

사용자 요구 원문:
> *"막 2020년 데이터를 보고 가장 최근 데이터가 2020밖에 없으면 **최근 확인된 자료는 없고 2020데이터는 이렇게 말하고있다** 이렇게 가야되는데, 26년에 질문했는데 2020년 데이터를 그냥 말해버려선 안되."*

| 상태 | 의미 | 화면 문구 |
|---|---|---|
| 🟢 `live` | 최근 갱신됨 | "2026년 7월 기준 최신 자료입니다" |
| 🟡 `stale` | 자료가 있으나 그 뒤는 **모름** | "가장 최근 확인 자료는 2020년 3월 기준입니다. **이후 상황은 확인되지 않습니다**" |
| 🔒 `frozen` | 활동 자체가 종료 → **"이후 데이터는 없다"고 단정 가능** | "2015년 12월 기준이며, **이후 데이터는 존재하지 않습니다** (2016-02-10 개성공단 전면중단으로 미생성)" |

- `stale`과 `frozen`의 구분이 이 서비스의 정체성이다. `stale`은 *모른다*, `frozen`은 *없다*.
- **`frozen`은 반드시 이유를 동반**한다 — DB 제약으로 강제: `check (freshness <> 'frozen' or frozen_reason is not null)`.
- **주제 단위 frozen 공지**가 데이터셋 단위보다 우선한다. 예: "개성공단 근로자 수"는 통계가 없어도, **주제가 종료**됐으므로 먼저 그것을 말한다.
- **as-of 감쇠**: 랭킹 가중치 `0.5^(경과연수/6)`. 단 **`frozen`은 감쇠 면제** — 그게 최종 확정값이기 때문.

## 3a. 근거/검증대상 분리 (구조로 강제)

사용자 지시: *"뉴스는 검증대상이 맞지. **통일부가 발표한게 팩트**인거고. 관련 뉴스는 링크만 보여주면 돼."*

- `verdict_citation.record_id` → **`record`만 FK**. 뉴스는 근거로 인용할 **문법 자체가 없다**.
- `record.kind` 체크 제약에 `'news'` 없음: `('event','entity','stat','doc','briefing')`.
- 관련 뉴스는 `verdict_related_news`로 **표시만** 한다.

---

## 4. 데이터 (`scripts/nk-catalog.mjs` = 단일 진실 소스)

- **24종 적재 완료** + **API 5종 `pending` 사전등록**(통일부 백엔드 복구 시 `status: 'pending' → 'ready'`만 바꾸면 됨).
- 규모: 레코드 **12,690** · 수치 **37,912** · 엔티티 **721**.
- `TOPIC_STATUS`: `econ.kaesong`(frozen 2016-02-10) · `econ.kumgang`(frozen 2008-07-11) · `ik.talks`(dormant 2018-12-31).
- `briefing`(보도자료, `searchPriority: 100`)이 판단의 씨앗.

**⚠️ 파일명·메타데이터를 믿지 마라.** 금강산 파일은 `20201231`로 배포되지만 관광은 **2008-07**에 끊겼다.
→ 카탈로그에서 `coverageEnd: '2008-07-31'`로 **수동 확정**했다. 이런 항목을 자동 추론으로 되돌리지 말 것.

**수집 원칙**: 질문마다 API를 때리지 않는다. 사용자 지시 — *"옛날 데이터는 계속 쌓아두고 최신꺼 갱신해서 우리 데이터에 추가하는 쪽으로."* → 배치 수집 + append-only.
- `fact_key`가 같은 사실의 여러 시점 스냅샷을 잇는다 → 증분 갱신 + "입장 변경 추적"이 공짜로 나온다.

---

## 5. 아키텍처

```
사용자 질문(비정형)
   │
① 시간표현 분리      nk-time.mjs      '최근/요즘/2019년쯤' → 닫힌 슬롯 7종 + 검색어에서 제거
② 질의 정규화        nk-normalize.mjs 규칙 우선, 신뢰도<0.34일 때만 LLM
   └ LLM 중간계층    nk-llm.mjs       Gemini. 스키마 밖 출력이면 폐기→규칙 결과로 되돌림
③ 도메인 게이트      nk-search.mjs    어휘/주제/엔티티/희소토큰 4신호 — 밖이면 즉시 무근거
④ 검색·랭킹          nk-search.mjs    BM25(K1=1.2,B=0.75,제목×3) + 결정적 가중
⑤ 답변 조립          nk-search.mjs    수치대조 / 집계·분포 / 연혁 / 근거그룹
   │
as-of 배지 + 출처 + 면책 (항상 동반)
```

**LLM 4원칙 (위반 금지)**
1. 규칙이 먼저. LLM은 규칙이 못 잡은 것만.
2. LLM은 **해석**만 한다. **판정·수치·근거 생성 금지.**
3. 출력이 닫힌 스키마 밖이면 **폐기**하고 규칙 결과로 되돌린다.
4. **네트워크가 죽어도 규칙 계층만으로 동작한다.**

> 사용자 지시: *"단위를 너무 명확하게 딱 정해버리면 같은 의미인데 표현법이 다른거 … 없는 정보라 뜰수도 있어서. **중간계층에 ai를 넣는거지.**"*

**단위 가족**(person/count/money/krw/weight/ratio) — **가족이 다르면 대조를 거부**한다.
사용자 확인: *"명 주장에 퍼센트 지표를 대조하지 않는거는 잘하는 부분이야."*
대신 **관련 정보**로 제시한다 — *"몇 번인지는 확인되지 않으나, 참고할 만한 자료로 …"*
(사용자 교정: *"번→명 **허용**이 아니라 **관련정보**잖아"* — '허용/대체'라는 표현을 쓰지 말 것.)

---

## 6. 스택 (FactDoc에서 그대로 승계)

| 레이어 | 선택 |
|---|---|
| 프론트 | Vite + React 19 + TS |
| DB | **Supabase** Postgres (pgvector · ltree · pg_trgm) — 프로젝트 `fcezfdnymianzxrezkdf` |
| 호스팅 | **Cloudflare Pages** |
| LLM | **Gemini** (`gemini-2.5-flash` → `2.0-flash-lite` → `2.0-flash` 폴백, 키 5개 로테이션, `thinkingBudget: 0`) |
| 배치 | GitHub Actions cron |

⚠️ **GitHub Actions 워크플로 5개가 아직 질병청 데이터를 커밋한다.** 비활성화/재조준 필요. 로컬이 origin보다 50커밋 뒤처져 있음.

---

## 7. 검증 하니스 (구체성 20점의 증거)

```bash
node scripts/eval.mjs       # 회귀 48건 — 현재 48/48
node scripts/wild.mjs       # 실사용 난문 52건 — 현재 52/52
node scripts/verify-nk-db.mjs  # DB 스키마·적재 18건 — 현재 18/18
node scripts/shot.mjs <디렉터리>  # 화면 13종 캡처 + 가로 오버플로 감사
```

### ⚖ wild-set 은 양방향 감시다 — 한쪽만 보면 반드시 무너진다
- **과잉회피**(이 프로젝트가 갈아엎은 실패): 개떡같이 물어도 답해야 한다. `북한 요즘 뭐함` 이 빈손이면 실패.
- **과잉주장**(그 반대 실패): 무관한 걸 근거로 포장하면 안 된다. `북한 핵` 에 인물 카드가 붙으면 실패.

두 감시가 **같은 세트 안에** 있다. 한쪽만 강화하면 문턱이 한 방향으로 밀려 반대편이 깨진다.
관련성 문턱을 건드릴 때는 반드시 양쪽을 함께 확인하라.

두 세트를 **분리**한 이유 (사용자 지시):
> *"너도 **너무 정직하게 물어보면 안되**. 사람이 하는 질문은 형식·문법·어휘가 많이 차이나잖아."*
> *"**바라는 답을 정해놓고 그걸 유도하듯 질문하는게 아니라**, 그냥 사람처럼 아무렇게나 질문해도 대응할수있는지를 평가하는게 맞고."*

- `eval-set.mjs` = 정답 고정 회귀. `wild-set.mjs` = **행동 단언**(정답을 못 박지 않고 "종료 공지가 떠야 한다" 식으로 검사).
- **새 기능을 넣을 때 wild-set에 유도형 질문을 추가하지 마라.** 반드시 사람이 실제로 칠 법한 형태로 쓴다.

> 사용자 인식: *"이 분석 시스템이 한번에 바로 나오진 않을거야. 수백번 테스트를 거쳐야 쓸만한게 하나 나올까 말까."* → **측정 없는 개선을 하지 말 것.**

---

## 8. 실행

```bash
node scripts/ingest-nk.mjs        # 북한자료/ CSV → frontend/src/data/nk-index.json (15.1MB)
node scripts/build-web-index.mjs  # → frontend/public/nk-index.json (13.5MB, gzip 0.9MB)
node scripts/eval.mjs && node scripts/wild.mjs
npm --prefix frontend run build
```
개발 서버는 `.claude/launch.json`의 `sasilon`(포트 5178)을 쓴다.

---

## 9. 가드레일

1. **as-of 없는 답변 금지.** 모든 수치·서술에 기준일 배지가 붙는다.
2. **`frozen`에는 이유 필수** (DB 제약).
3. **뉴스는 근거가 아니다** — 통일부 공식자료만 근거. 뉴스는 링크만.
4. **단위 가족이 다르면 대조 거부** → 관련 정보로 제시.
5. **근거 없으면 억지 판정 금지** → "관련 통일부 공식 자료를 찾지 못했습니다"를 1급 결과로. *단, 이게 §2의 '확인이 어렵다' 도피로 퇴화하지 않도록 wild-set으로 감시한다.*
6. **도메인 밖 질문**(치킨·날씨)은 답하지 않는다.
7. 북한 관련 정보 특성상 공식자료 미수록 사실이 존재할 수 있음을 항상 면책 고지.

---

## 10. 남은 작업

`전환-작업지도.md`에 상세. 요약:
- [ ] **③ Supabase 적재** — `0011`/`0012` 마이그레이션 적용 + 12,690/37,912/721 로드
- [ ] GitHub Actions 워크플로 5개 비활성화·재조준
- [ ] 뉴스 레이더(`nk-issue-radar.mjs`)를 `news_item`/`issue` 테이블에 연결 (TOP 10 정밀도 현재 ~50% — 노이즈 필터 개선 필요)
- [ ] 통일부 API 복구 시 `pending` 5종 → `ready`
- [ ] **서식3 기획서 작성** (A4 10쪽 이내)
