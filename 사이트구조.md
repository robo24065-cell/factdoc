# 고향잇기 사이트 구조 (IA) — 확정본

> 사용자 지시(2026-08-19): *"사이트 구조를 만들때. 실 이용자가 접근하기 편하게 만들어야해. 보기쉽고."*
> 이 문서가 라우팅·내비게이션의 단일 진실 소스다. 바꾸려면 이 문서부터 바꾼다.

## ⚠ 개정 (2026-08-19 저녁, 사용자 지시) — 홈은 「씬 스크롤 서사」다

사용자 지시 원문: *"스크롤 내리다보면 한 부분에 멈춰서 그쪽 콘텐츠 죽 가로든 새로 플립이든 다 끝나고
또 아래로 내려가는 그런 형식"*, *"어짜피 저런 스크롤 하면 한 정보씩 띄우는 페이지면 이제 정보 밀도가
낮아지는데 배치를 잘 하라는거지"*

- 홈(`/`)은 아래 표의 페이지 분할 대신 **한 스크롤 씬 서사(5막 13씬)** 로 간다.
  한 뷰포트에 한 생각(숫자 1·질문 1·그림 1)씩. 밀도는 씬이 조절하고, 정보 삭제는 0건.
- 씬으로 승격하는 기준: ① 독립된 질문에 답하고 ② 첫인상 요소가 홀로 설 때. 가정·한계·출처 줄은
  씬의 「꼬리」(캡션급)로 격하하되 접지 않는다. 무대 위 숫자는 최대 3개.
- 핀(sticky) 가로 넘김은 **사료 사진(S6)·신청 창구 덱(S11) 두 곳만**. 스크롤 하이재킹 금지 —
  네이티브 sticky + scroll-snap 행 + 좌우 단추(≥48px) + 키보드. `prefers-reduced-motion`이면
  전부 꺼지고 평범한 세로/가로 snap 배치로 남는다.
- 아래 하위 페이지 표(`/map` `/clock` `/action` `/archive`)는 **직행 주소로 존치**하며
  현재는 홈의 해당 씬으로 리다이렉트한다. 나중에 독립 페이지로 되살릴 수 있게 표는 지우지 않는다.
- 한걸음씩 모드는 이 개정의 영향을 받지 않는다(이미 저밀도).

---

## 원칙 — 한 페이지는 한 가지 질문에만 답한다

지금까지는 모든 것이 `/` 한 페이지에 쌓여 있었다. 지도·기록 골든타임·후손층·출처가
세로로 이어져, 처음 온 사람은 **벽**을 마주한다. 실사용자(평균 83.0세, 그리고 그 손주)가
들어와서 3초 안에 "내가 갈 곳"을 찾지 못하면 나간다.

그래서 쪼갠다. 각 페이지는 사람의 질문 하나에만 답한다:

| 경로 | 답하는 질문 | 내용 |
|---|---|---|
| `/` | **여기는 뭘 하는 곳인가?** | 큰 숫자 하나 + 고향 버튼 7개 + 입구 카드 3개. 그게 전부 |
| `/map` | **내 고향은 어떤 곳인가?** | 지도 + 지역 패널(날씨·통계·기록·사료) |
| `/clock` | **몇 년 남았나?** | 기록 골든타임 + 통일의식 추이(출처 구분 배지) |
| `/action` | **후손인 나는 뭘 할 수 있나?** | 기증(최상단)·유전자검사·신청 등 10개 경로 + 아직 닫힌 것 |
| `/archive` | **사진·기록을 보고 싶다** | 박물관 사료 4,342건 열람(지역·컬렉션 필터) |
| `/factcheck` | **궁금한 게 있다** | 사실은ON 팩트체커 (기존) |
| `/sources` | **이 수치들 어디서 왔나?** | 데이터셋 전체 + 기준일 + 원문 링크 |

리다이렉트: `/gohyang` → `/` (공유 링크 보존).

## 내비게이션

**데스크톱** — 머리글 밑줄형 가로 주메뉴 4개(정부 누리집 관용):
`고향 지도 · 기록 골든타임 · 후손 안내 · 팩트체커`
(archive는 지도 패널과 홈에서 진입, sources는 바닥글에서 — 주메뉴는 4개를 넘기지 않는다)

**모바일** — 하단 고정 탭 4개, 라벨 포함, 터치 영역 56px.
엄지가 닿는 곳에 있어야 한다. 상단 햄버거 메뉴는 노인 사용자에게 보이지 않는 것과 같다.

**공통**
- 모든 하위 페이지 상단에 경로 표시(`홈 > 고향 지도`)와 「홈으로」.
- 「한걸음씩」 모드 전환은 모든 페이지 머리글에 상주. localStorage 유지.
- 바닥글: 출처(`/sources`) · 시제품 고지.

## 홈 화면 상세 — 선택지를 셋으로 제한한다

```
[큰 숫자]  고향을 기억하는 사람이 33,272명 남았습니다
           2026년 5월 기준(게시판 공표 HWP)
           ─ 평균 나이 83.0세는 **다른 축**이다: 등록현황 월별 자료 2025년 8월 기준.
             한 줄에 묶어 「2026년 5월 기준 · 평균 83.0세」라고 쓰면 as-of 규약 위반이며,
             이 프로젝트 자신의 검증기(validateGuide)가 폐기하는 문장이 된다.

[물음]     어느 고향을 찾으십니까?
           [황해도] [평안남도] [평안북도] [함경남도] [함경북도] [경기(미수복)] [강원(미수복)]
           → 누르면 /map 으로, 해당 지역이 열린 채

[입구 카드 3개 — 크게]
  ① 고향 지도       그 고향의 오늘 날씨·역사·사진
  ② 기록 골든타임       몇 년 남았는지, 계산 결과
  ③ 후손이 할 수 있는 일   기증·유전자검사·신청 (10가지)

[한 줄]   지도에 없는 것은 팩트체커에 물어보세요 →
```

홈에는 이 이상 아무것도 두지 않는다. 통계 나열·뉴스·설명 문단 금지.
**노인 UI의 제1원칙은 "선택지를 줄이는 것"이다.**

## 페이지 이동이 데이터 흐름과 일치해야 한다

고향 버튼(홈) → `/map?r=함경남도` → 패널의 사료 더 보기 → `/archive?r=함경남도`
→ 사료 카드의 "박물관에서 보기" → 박물관 원문(새 창) → 돌아와서 → `/action` 기증 카드.

이 동선이 서비스의 척추다: **고향 이름 하나 → 보기 → 행동(기증)**.

## 구현 메모

- `GohyangOn.tsx`(현 1,800줄)를 페이지별로 분해: `Home.tsx(고향잇기용 신규)` ·
  `MapPage` · `ClockPage` · `ActionPage` · `ArchivePage` · `SourcesPage`.
  공용 조각(AsOfPill·Block·RegionPanel·지도 SVG)은 `frontend/src/components/gohyang/` 로.
- 쿼리 파라미터로 상태 전달(`/map?r=지역`) — 뒤로가기·공유가 자연스럽게 동작해야 한다.
- 데이터 팩은 페이지별로 필요한 것만 fetch (홈은 isan.json 요약만 → 첫 화면이 가벼워진다).
- 한걸음씩 모드는 라우팅과 독립 — 어느 페이지든 그 페이지의 내용을 카드 단위로 풀어낸다.
