# 심사기준 자가 대조 — 고향ON

> 안내문 원문 배점: 적합성 30 · 창의성 30 · 구체성 20 · 효과성 20
> 작성 2026-08-19. **점수는 자평이며, 근거는 전부 실측치다.**

---

## 적합성 30 — "통일부 공공데이터를 아이디어에 얼마나 적절하게 활용했는지"

**자평 27~29 / 30**

근거:
- 통일부 데이터 **31개 데이터셋 · 68,487건**을 실제 적재했다(추정 아님, 인덱스 실측).
- 이 서비스의 **심장이 통일부 데이터**다. 빼면 서비스가 성립하지 않는다:
  - 소멸 시계의 모집단 = 통일부 이산가족 신청현황(2026-05-31 기준 33,272명)
  - 고향 지도의 두 축 = 통일부 이산가족 출신지 7종 + 통일부 탈북민 재북 출신지 13종
  - 지역별 기록 = 통일부 연표 8,969 · 보도자료 2,709 · 북한동향 42,813 · 북한개황 93
  - 사료 = 통일부 남북이산가족 디지털박물관
- 외부 데이터는 **전부 보조**이고, 각각 역할이 한정돼 있다:
  통계청 완전생명표(추계 방법) · Open-Meteo(오늘 날씨) · 서울대 통일평화연구원(참고 지표).

**감점 위험과 대응**
- 위험: 통계청 생명표가 소멸 시계의 "방법"이라 외부 데이터가 주인공처럼 보일 수 있다.
  → 대응: 기획서에서 **"모집단·연령분포·성별은 전부 통일부 값이고, 생명표는 곱해지는 계수일 뿐"**임을 명시한다.
- 위험: 서울대 통일의식조사는 통일부 데이터가 아니다.
  → 대응: 화면과 기획서 모두에서 **출처가 다름을 배지로 구분**하고, 보조 축으로만 쓴다.

---

## 창의성 30 — "아이디어의 독창성 및 차별성"

**자평 25~27 / 30**

근거:
1. **as-of 3상태 모델** — 🟢live / 🟡stale / 🔒frozen.
   `stale`은 *모른다*, `frozen`은 *없다*. 이 둘을 구분하는 서비스를 찾지 못했다.
   frozen에는 이유를 강제한다(DB CHECK 제약으로 구조적으로 강제).
2. **소멸 시계** — 통일부는 이산가족 통계를 매월 공표하지만
   "이 속도면 몇 년 남았는가"를 계산해 보여주지 않는다. 그 빈자리를 채운다.
   단일 수치로 단정하지 않고 **두 시나리오 범위**로 낸다(1만 명 하회 2038~2041년).
3. **고향 좌표계** — 이산가족 출신지(광복 당시 구행정구역 7종)와
   탈북민 재북 출신지(현행 13종)를 **한 지도 위 두 좌표계**로 겹쳤다.
   지금까지 이 둘은 서로 다른 표에 따로 있었다.
4. **후손 다리** — 통일부 실태조사에 「후손 세대」 섹션이 있는데
   그 데이터를 쓰는 서비스가 없다. 진단(간극 3종)에서 그치지 않고 행동(기증 등)으로 연결한다.
5. **두 밀도** — 같은 데이터를 「한눈에」와 「한걸음씩」 두 모드로.
   실사용자 평균 나이가 83.0세라는 사실에서 나온 설계다.

**감점 위험과 대응**
- 위험: **"또 지도냐"** — 2025년 장려상 「동행 MAP」과 형식이 겹쳐 보인다.
  → 대응: 첫 페이지에서 끊는다. 동행 MAP은 *탈북민 정착 지원시설* 지도이고,
    고향ON은 *이산가족의 고향*이 축이다. 모집단·축·목적이 전부 다르다.
- 위험: 「기억 계승」 소재 자체는 통일부가 이미 디지털박물관으로 하고 있다.
  → 대응: 우리는 박물관을 대체하지 않고 **진입로**를 만든다.
    차별점은 소재가 아니라 **계량(소멸 시계)과 연결(고향 좌표계)**이다.

---

## 구체성 20 — "현실적으로 구현 가능하고 구체적인지"

**자평 18~19 / 20**  ← 가장 강한 축

근거:
- **작동하는 시제품이 있다.** 아이디어 부문에 필수는 아니지만 만들었다.
- 회귀 검증이 자동화돼 있다: `eval.mjs` **48/48**, `wild.mjs` **108/108**.
- 데이터 수집이 전부 **재실행 가능한 스크립트**다.
  `nk-isan-harvest.mjs`(등록 98개월 + HWP 파싱) / `nk-isan-project.mjs`(KOSIS 생명표) /
  `nk-build-map.mjs`(Natural Earth) / `nk-build-region.mjs` / `nk-gohyang-pack.mjs`.
- 데이터 정합을 스스로 검증한다: 생존자 5개 분류 합 = 33,272 전부 일치,
  개요 항등식 33,272 + 101,274 = 134,546, 교류현황 38개 연도행 열합 = 공표 합계.
- 원자료의 오차도 **고치지 않고 기록**했다(2021-04 남녀합 1명 차 등).

**감점 위험과 대응**
- 위험: 박물관 이미지를 우리가 저장하지 않고 원본을 참조한다 → 통일부가 막으면 깨진다.
  → 대응: 실패 시 이미지 자리를 숨기고 제목만 남기도록 구현했고, 기획서에 **공식 연계(API) 제안**을 적는다.

---

## 효과성 20 — "도입 시 예상되는 기대효과"

**자평 15~17 / 20**  ← 가장 약한 축. 보강 필요

근거(강한 것):
- **통일부 자신의 조사가 이 서비스를 요구했다.** 제4차 실태조사(2024, 심층 5,103명)에서
  이산가족이 1순위로 꼽은 사업이 「사진·물건 등 기록물 수집 보존」 **59.9%**,
  위로사업 2위가 「고향 관련 사진·영상의 수집·제작·전시」 **44.5%**다.
- 후손 수요가 실재한다: 1세대 사후 세대 간 교류 희망이 후손 **55.7%** vs 1세대 37.7%.
- 대상 규모가 3만이 아니다: 1세대 누계 134,546명 중 79.2%가 자손 보유 → **2세대만 21만~43만 명**.

약한 것(정직하게):
- **"몇 명이 실제로 쓸 것인가"에 대한 답이 아직 약하다.** 이용자 수 추정 근거가 없다.
- 정책 개선으로 이어지는 경로가 서술적이다. KPI가 필요하다.

**보강 방향** — 기획서에 다음을 넣는다:
1. 측정 가능한 목표: 디지털박물관 사료 열람 수, 기증 문의 건수, 고향별 페이지 체류.
2. 통일부 업무계획의 「이산가족 생애기록 아카이브 구축 기반 강화」와의 접점을 명시.
3. 소멸 시계가 **예산·사업 우선순위 근거**로 쓰일 수 있음을 구체적 문장으로.

---

## 종합

| 지표 | 배점 | 자평 | 상태 |
|---|---:|---:|---|
| 적합성 | 30 | 27~29 | 강함 — 통일부 데이터가 심장 |
| 창의성 | 30 | 25~27 | 강하나 **중복 인상 방어** 필요 |
| 구체성 | 20 | 18~19 | 가장 강함 — 실측·회귀·재실행 |
| 효과성 | 20 | 15~17 | **가장 약함 — 정량 목표 보강** |
| **합계** | **100** | **85~92** | |

### 지금 당장 해야 할 세 가지
1. **효과성 정량화** — 측정 가능한 목표 3개를 기획서에 못 박는다.
2. **중복 방어를 1페이지로** — 2025 수상작과 무엇이 다른지 표로 먼저 보인다.
3. **생성형 AI 활용 내역** — 서식이 요구하는 필수 기재 항목이다(§기타 참고 사항).
